org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Enable Gradle build cache for faster builds
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.configureondemand=true

# Enable build cache for Android (deprecated option removed)
# android.enableBuildCache=true  # Removed in Android Gradle Plugin 7.0+

# Enable R8 full mode for better optimization
android.enableR8.fullMode=true

# Enable non-transitive R classes for faster builds
android.nonTransitiveRClass=true